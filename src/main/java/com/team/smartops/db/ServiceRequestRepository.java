package com.team.smartops.db;

/**
 * OWNER: Team B (Database).
 * Reads/writes ServiceRequest records between the database and CSV seed files,
 * and hands loaded records to Team C's structures for in-memory use.
 */
public class ServiceRequestRepository {
    // TODO: insert/select/update methods for servicerequest
}
