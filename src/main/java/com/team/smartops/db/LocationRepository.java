package com.team.smartops.db;

/**
 * OWNER: Team B (Database).
 * Reads/writes Location records between the database and CSV seed files,
 * and hands loaded records to Team C's structures for in-memory use.
 */
public class LocationRepository {
    // TODO: insert/select/update methods for location
}
